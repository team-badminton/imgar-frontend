import styled from 'styled-components';

export const SelectBoxHeaderButton = styled.button`
  color: ${({ theme }) => theme.color.white};
  font-size: ${({ theme }) => theme.fontSize.xl};
  font-weight: 700;
`;
